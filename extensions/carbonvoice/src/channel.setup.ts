import type { SetupPlugin } from "openclaw/plugin-sdk/compat";

export const carbonVoiceSetupPlugin: SetupPlugin = {
  id: "carbonvoice",
  name: "Carbon Voice",
  description: "Configure the Carbon Voice channel",
  // Setup surface and config editors will be implemented in a later iteration.
};

